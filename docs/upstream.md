# Upstream notes — what ao borrows, from where, and how to re-sync

ao carries no runtime dependency on any of these projects. Borrowed behaviour is
ported into ao's own standard-library code; the entry here pins what was read,
so a later re-sync is a diff against a known commit, not a guess.

| Project | Licence | Pinned | Borrowed (ported, not linked) | Deliberately not borrowed |
|---|---|---|---|---|
| [affaan-m/ECC](https://github.com/affaan-m/ECC) | MIT | `e04ea0b9cc82` (main, 2026-09-03); release v2.2.0 | AgentShield's *check categories* for agent configuration files — secrets in agent files, overly permissive allow rules, missing deny list, hook safety, MCP package hygiene — re-implemented natively behind `ao doctor` (backlog #14); the "turn a repeated win into a skill" idea (instincts) as an `ao` lessons→playbook proposal; a Kiro install target contributed upstream (backlog #15) | the 286-skill/68-agent content pack, hooks, the `ecc-universal`/`ecc-agentshield` npm packages as dependencies |
| [Fredrin](https://fredrin.com/) | proprietary, closed source, cloud control plane | site read 2026-09-07 (landing + [pricing](https://www.fredrin.com/pricing)) | **Ideas only, nothing ported yet.** Its goal→ticket→dependency model: a goal holds tickets, the tool derives the dependency order and runs every unlocked ticket in parallel. That is ao's weakest surface — our board is hand-maintained prose and READY is judged by a human reading it (backlog #33–#35). Also: one board visible from desktop, browser and phone, and BYO subscription with model calls going straight from the machine to the provider (which is already ao+keyflip's split) | The cloud control plane and hosted board — ao is local-first and the umbrella platform's recorded invariant is no central identity. "Review and auto-merge" as a product feature: landing without evidence of an independent review of the exact candidate is the failure ao exists to prevent. Also its agent-usage billing, which duplicates keyflip |
| [Bernstein](https://github.com/andyrewlee/awesome-agent-orchestrators) and the goal→DAG orchestrators surveyed with it | mixed, mostly MIT | surveyed 2026-09-07 via awesome-agent-orchestrators | One principle worth keeping: **scheduling decisions are made in code, never by a model** — same inputs, same order, however the agents' replies interleave. ao already holds this (`ao features` all-off is deterministic); the decomposition work in #33–#35 must not break it by asking a model what is READY | Their planner agents deciding task order at runtime, and merge steps that land work without an authority grant |
| [iokaio/munarium](https://github.com/iokaio/munarium) | Apache 2.0 | read 2026-09-08, v1.0.0 (6 stars, 7 commits) | **Ideas only.** Two framings worth keeping: governance enforced at *write* time rather than read time (ao already does this — a grant is issued when the candidate is staged, not when someone asks), and every retrieval carrying a provenance envelope naming the evidence it rested on. The second generalises what ao does for reviews and grants but not for its own answers: `ao status`, `digest` and every notice should be able to name the ledger rows behind them (#37, #43) | The implementation. It needs Docker, PostgreSQL 16 and pgvector; `pyproject.toml` says `dependencies = []` on purpose, because ao watches agents on machines it does not control and `ao status` must work before anything is installed. Making the authority ledger — the one component we just made tamper-evident — depend on a network service at v1.0.0 with 7 commits would trade a proven local guarantee for an unproven remote one |
| [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail) | MIT | read 2026-09-08 (130,858 stars, 7,010 forks, created 2026-06-12, pushed 2026-09-07) | Its decision ladder — need it at all, already in the repo, in the standard library, one line, only then a minimal implementation — rewritten in our own words and adapted for this project as "Before adding a mechanism, walk the ladder" in [slices.md](slices.md), with two rungs added from our own history: ordering instead of machinery, and deleting instead of adding. Directly aimed at what cost the most here: B8 grew a mechanism per review round for twelve rounds | The package itself. It is prompt-injected rules and hooks for 20+ harnesses; ao already owns its steering surface and injecting a second, unversioned rule set into the implementer's prompt would compete with the acceptance boundary rather than support it. No npm dependency, no hooks imported |
| [Traycer](https://traycer.ai/) | proprietary, VS Code extension | read 2026-09-08 | **The strongest reference we have for the one thing ao does worst.** It takes a high-level intent, decomposes it into ordered phases with a detailed implementation plan per phase, hands each off to a coding agent, and verifies the result against the plan. The owner uses it and rates it highly at exactly this — that first-hand assessment is why it is recorded here rather than dismissed. ao's equivalent is a hand-maintained markdown board where a human reads prose to decide what is actionable; #33 (READY computed from a dependency graph), #34 (size measured at registration) and #35 (a path boundary proven possible before work starts) should be built with Traycer's phase model as the reference for what good looks like. It supports this project's implementer natively — a first-class Kiro provider authenticated by login or `KIRO_API_KEY`, alongside Claude Code, Codex, Cursor, Grok, Copilot and its own inference — so there is no integration cost at all; it is a desktop application with its own worktree, session and per-provider MCP/skill management, which means it overlaps ao more than its extension description suggests. **Recorded use:** as a one-shot planner beside ao, never above it — hand it a body of work (the audit findings), take the phase breakdown it produces, and turn those phases into ao slices by hand. No runtime coupling, and the implementer keeps taking instructions from one place | Adopting it as the layer above ao. Two planners over one implementer is the two-judges failure this project keeps paying for — it happened twice on 2026-09-07 alone — and Traycer's "verification" is a quality check against a plan, not an authority gate: it has no grant bound to an exact staged candidate, no enforced reviewer independence, no tamper-evident ledger, no waiver on the record. It would not have caught any of the sixteen BLOCKERs the 2026-09-08 audit found. Planning is not this project's bottleneck; authority correctness is |
| [RFC 6962](https://www.rfc-editor.org/rfc/rfc6962.html) / [transparency-dev/merkle](https://transparency.dev/articles/logs-a-verifiable-transport-layer/) | RFC / Apache 2.0 | read 2026-09-08 | **The design, for #62.** Our hash chain detects modification and insertion but not erasure, because validation walks forward from a genesis row — so any prefix of `authority.jsonl` validates and deleting the tail is invisible. Transparency logs solved exactly this: a **checkpoint** committing to the tree size and root hash, and a consistency proof that one log is a prefix of another. Adopting the shape — each row's ordinal, plus a periodically written checkpoint of (count, root) that `commit-check` and `doctor` compare against — turns a truncated ledger into a broken commitment instead of a shorter valid chain. SHA-256 is in the standard library, so this costs no dependency | The Go libraries and the server-side world around them: witnesses, cosigners, monitors, a log to publish to. ao is local-first and single-machine; a public log is a different product |
| [in-toto attestation v1](https://github.com/in-toto/attestation) | Apache 2.0 | spec read 2026-09-08 | **The statement shape, for grants and reviews.** An ao grant already is an attestation in everything but format: a subject (the exact candidate digest), a predicate (verdict, reviewer identity, gate results, waivers), and a statement binding them. in-toto v1 standardises that envelope, and SLSA provenance is one predicate inside it. Emitting our receipts in that shape costs nothing we are not already recording and makes them readable by tooling built for supply-chain provenance — worth doing when #61 reshapes verification records anyway | DSSE signing and the key management it implies, cosign, OCI registries. ao's threat model concedes same-user forgery is detectable rather than preventable; adding a signing key stored on the same machine would dress that up without changing it. Adopt the shape, and say plainly that the statements are unsigned |
| [gitleaks](https://github.com/gitleaks/gitleaks) | MIT | read 2026-09-08 (25k★, single Go binary) | **The rules, for #48.** Over a hundred credential patterns in TOML with per-rule entropy thresholds and allowlists, covering the shapes that would actually leak through a review artefact or a mail file — cloud keys, tokens, private keys, JWTs. Porting the pattern set and the regex-plus-entropy structure is the same move already made for AgentShield's check categories: behaviour ported natively, pinned here, no dependency | The binary and the git-history scanning it exists for. ao needs a check on the bytes it is about to write, not a repository scanner |
| [`graphlib`](https://docs.python.org/3/library/graphlib.html) (Python standard library) | PSF | verified on 3.9.6, this project's floor | **`TopologicalSorter`, for #33.** Dependency-ordered READY with cycle detection is in the standard library: `prepare()` raises `CycleError` carrying the cycle, which is exactly the error message #33 asks for. The algorithmic core of the board-as-graph work needs no code of ours and no dependency — confirmed importable on the 3.9 floor | Nothing. This is a straight adoption; the only work is mapping board rows onto it |
| [Hypothesis](https://hypothesis.readthedocs.io/) | MPL 2.0 | read 2026-09-08 | **The shrinking idea, for #11 — not the package.** Its real contribution is not random input but *shrinking*: turning a failure into the smallest case that still fails, which is the difference between "seed 41827 fails" and a two-line reproducer. #11's acceptance is already written as a seeded loop (`randomised over N seeds … the seed printed`), so the loop itself is `random` from the standard library; what we take is the shrinking step — on a failure, narrow the World by removing one element at a time and keep the smallest that still contradicts the invariant. Perhaps fifty lines, ours, and the seed still reproduces it | The dependency. It is genuinely good and would be *only* a test extra, but adding it would make the suite unrunnable where the package is missing, and this project is written for machines where nothing is installed. If the hand-written shrinker turns out to be the wrong economy, revisit it then — with the loop already in place, swapping it in is a small change |
| [hakkisagdic/plansmith](https://github.com/hakkisagdic/plansmith) (+ `-analyzer`, `-desktop`) | MIT (desktop: Apache-2.0, a debranded fork of traycerai/traycer) | read 2026-09-09 | **The owner's own code, so read it freely.** It already contains, in TypeScript, two things ao has only written down: `packages/plugin-host` and `packages/protocol` — the reference for #77's adapter contract, an existing answer to "how does a third party ship one" — and `plugins/context-analyzer`, `context-symbols`, `context-fs`, `context-mcp`, which decompose codebase questions the way #81 should. `handoff-cli-generic` is the same idea as ao's adapters. Read the protocol and plugin-host for the contract shape; read the context-* split for #81's provider boundary | Porting it. ao is zero-dependency Python on a 3.9 floor and plansmith is a pnpm monorepo with Docker and k8s; the value is the design, taken as design. Also not running it as a parallel product right now — one implementer and a backlog this long is exactly the two-planners mistake in another costume |
| [hakkisagdic/ctxman](https://github.com/hakkisagdic/ctxman) | MIT | v3.0.0, read 2026-09-09 | **The first provider for #81.** Method-level token counting and filtering across 14+ languages, with a REST API and watch mode — the structured extraction a codebase question needs as input, already published to npm. ao calls it as a configured provider behind the content seam; the citations it can produce are what make an answer checkable rather than plausible | Embedding it or depending on it. It is Node, ao is dependency-free Python: absent the provider the feature is absent and says so, which is the same contract as keyflip for quota |

## Re-sync procedure

1. `git -C <ecc checkout> diff <pinned>..origin/main --stat -- packages/agentshield src/` and read only the
   parts that touch a borrowed category.
2. Port a change only when it maps to an ao invariant; update the pinned commit here in the same commit.
3. Never vendor files verbatim; ao stays dependency-free and its tests own the behaviour.

## Known false positives of AgentShield (kept out of the port)

- Reversed-text heuristic flags the phrase "backward compatible".
- `env -u VAR cmd` (unsetting a variable) is reported as "dumps environment variables".
- A deny rule that *mentions* `--no-verify` is reported as a dangerous flag; the port must inspect the
  rule's list (allow vs deny) before scoring.

## Overlap watch

- ECC 2.0 (`ecc2/`, Rust alpha) is a multi-session control plane: SQLite session store, daemon, worktree-aware sessions, risk scoring, orchestration and review controls. That is ao's layer, not the skills layer. Re-check on every re-sync; ao's differentiators stay authority, receipts and independent review of the exact candidate.
