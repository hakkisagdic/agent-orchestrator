# ao backlog — what is left, in order

Maintained by the architect. Every item is a slice with an acceptance boundary
written as an invariant, so an implementer can take it without a conversation.
Nothing here is authorised until it is queued on a project board.

| # | Slice | Why now | Acceptance boundary (invariant) |
|---|---|---|---|
| 8 | **Secondary project queue** | 80 "queue empty" reports in one day while another project had READY work | When a project's READY set is empty and its blockers wait on a human or architect, the nudge names the READY item of a configured secondary project instead of asking the implementer to wait |
| 9 | **Windows completeness** | `cwd` is unavailable on Windows, hold semantics untested there, no toast channel | Windows CI lane green on demand; `procs.cwd` via PEB or documented `None` handling in every caller; `ao hold` proven on Windows; toast notification channel behind a feature switch |
| 10 | **Measured feature costs** | `features.md` percentages are estimates | `ao cost` attributes implementer spend per feature from ledgers; the doc shows measured values with the sample window, and the estimate column is removed |
| 12 | **Release 0.5.0** | the installed tool already runs main; PyPI and Homebrew lag | After one landed slice under candidate-bound authority on Voltrai: version bump, changelog, tag, Homebrew formula; README EN and TR rows match the CLI |
| 13 | **Tandem mode (optional)** | the owner asked whether roles may rotate per slice or per project | Roles may rotate per slice on tool repositories; the invariant `reviewer ≠ author of the candidate` holds for every grant regardless of who holds which role; product repositories keep fixed roles (architect never implements beyond hotfixes) |
| 14 | **Harness content seam (borrow, pin, verify)** | the owner wants ao to carry the multi-harness approach; ECC (MIT) has 286 skills but installing it wholesale costs context and trust | `ao content add <source>@<pin> --skills a,b` vendors a curated, commit-pinned, checksum-verified subset of third-party skills into each harness's discovery directory (Claude skills, Kiro steering) through the existing adapter contract; nothing unpinned, nothing executable, hooks never imported; `ao doctor` ports AgentShield's check categories natively (no ECC/AgentShield dependency, no npm; false positives listed in docs/upstream.md are excluded) and reports findings as problems; upstream tracked by pinned commit in docs/upstream.md |
| 15 | **Kiro CLI install target for ECC (in our fork)** | ECC already ships a `.kiro/` layer (66 agents, 43 skills, 22 steering, 14 IDE `.kiro.hook` files, 3 guides) but its installer has no `kiro` target and the hooks are Kiro-IDE format; upstream PR #2287 (open) migrates hooks to the JSON v1 format Kiro CLI reads | Work happens in `hakkisagdic/ECC` (clone: `~/Projects/GitHub/ECC`, remote `upstream` = affaan-m/ECC); `install.sh --target kiro --profile minimal` writes only project-local `.kiro/` (steering auto-included, hooks converted to v1 JSON, agents as `kiro-cli agent` definitions, skills as steering), dry-run and regression tests keep every write inside `.kiro/`, IDE-only features are declared unsupported rather than faked; rebased on PR #2287 if it lands first; the owner reviews before any upstream PR; nothing is vendored into ao |
| 22 | **The secondary project is watched too** | 2026-09-07: while the implementer worked in an ao worktree, the Voltrai watchdog read the same agent as idle with a slice running and nudged it back to the primary project; `ao status` on Voltrai showed STOPPED while the agent was actively working elsewhere | Presence and idleness are measured per agent, not per project: an implementer working in a configured secondary project counts as working everywhere it is registered; the primary project's status names the project the agent is actually in; no nudge is spawned for an agent that is demonstrably working somewhere else |
| 25 | **No deadline on thinking; a deadline on silence** (pipeline A, docs/pipeline.md) | 2026-09-07: a 20-file / 5,592-line candidate was killed twice at the hardcoded `--timeout 900`, discarding ~30 minutes of model work, while a 5-file / 421-line candidate finished in 11m48s; the cap existed only because a blocked implementer was waiting, and #27 removes the waiting | A review has no total deadline and `--timeout` as a whole-review cap is gone; a section is killed only when it stalls — no output and no heartbeat for `review.stall_minutes` (default 10) — and is then recorded unanswered with its partial output kept as evidence, the rest of the review untouched; total work is bounded by the finite section count, and `ao reviews` shows cumulative elapsed and spend; a candidate over the slice budget (≤5 paths, ≤400 changed lines) is refused before any reviewer is spawned, printing measurement beside budget, because the answer to a too-large candidate is to split it (Z0–Z3) |
| 26 | **A review runs in sections and survives being cut off** (pipeline B, docs/pipeline.md) | 2026-09-07: a killed review discarded every answer it had already produced, twice; a review of eight scenarios is eight questions, not one | The prompt is split into sections (numbered scenarios in the boundary, else candidate file groups) and each result is appended to `.ao/reviews/R-<id>.jsonl` before the next section starts; `ao review resume R-<id>` re-runs only unanswered sections under the same id, tree and round; the verdict is computed from sections (any BLOCKER/HIGH → NEEDS_CHANGES, unanswered sections → no verdict, not a round); each section completion writes a heartbeat line that `ao reviews` shows (P1–P4) |
| 30 | **Unread age escalates on the existing ladder** | delivery-by-deletion records handling, never receipt: nothing distinguishes "not yet seen" from "seen, still working", so a message nobody opened looks identical to one being worked on, and neither raises anything | Each message carries a class (`fyi`, `needs-read`, `needs-decision`); the mailbox records `seen_at` when a recipient's command surfaces it, separately from deletion; an unseen `needs-decision` older than the configured yellow/orange/red thresholds escalates on the alarm ladder already in `ao alarms` (architect wake → desktop and Telegram → e-mail), and `ao status` names the oldest unseen message with its age; a test drives one message through all three thresholds |
| 31 | **Messages are addressed to roles, never to actor names** (docs/roles.md "Address roles, never names") | `urgent_messages` selects on a literal spelled from the architect actor's name, so a marked message reaches the architect only while that actor is the one named in the source; the same literal is in the watchdog's wake prompt, the hold-release filename and the Telegram bridge, and `ao note --to` / `ao decide --to` default to a hardcoded implementer name. Role reassignment is advertised as a first-class operation and this quietly breaks it | Every message carries `from_role` and `to_role` (architect, implementer, reviewer, watchdog, human); all selection, filtering and CLI defaults resolve through the role table, and actor names survive only as display text in filenames and reports; generated prompts name roles and glob on role, not on a proper name; adapter identity (`adapter: "kiro"`, the `kiro-cli` binary and its stores) is explicitly out of scope and unchanged; a test reassigns both actors to new names and proves urgent delivery, watchdog wake text and `--to` defaults all still address the right role, and a guard test fails if a role-actor literal reappears outside adapter code |
| 32 | **Quota rotation is asked of keyflip, not hand-written into config** (docs/keyflip.md integration point 2) | 2026-09-07: kiro-cli hit its overage limit and every nudge failed silently for an hour; the reviewer then shared one Claude window with the architect and `doctor` advised "add reviewer.fallbacks" — a hand-maintained argv list is provider routing, which docs/keyflip.md says ao must never reimplement; the fallback list was in fact edited by hand the same day | Before spawning any actor (reviewer, architect wake, implementer nudge) ao asks keyflip for that provider's headroom and rotates when the answer is "exhausted"; with no headroom anywhere it surfaces that to the human instead of spending the attempt, and an overage or quota error from a launched actor is reported as such rather than as silence; because keyflip switching is machine-wide and in-place, actors sharing a provider are serialised around a rotation rather than assumed to hold independent accounts; the `shared-pool` doctor check points at keyflip rotation, and `reviewer.fallbacks` remains only as the manual escape hatch for a machine without keyflip |
| 33 | **READY is computed from a dependency graph, not judged by reading prose** | `.ao/board.md` is hand-maintained text and `needs:` is free-form; on 2026-09-07 the architect decided what was actionable by reading it, the watchdog reported `queue-shallow` while three slices were genuinely blocked, and a dependency named in prose (`needs: B8b, HF-1`) was never checked against anything | `needs:` holds board item ids, validated on read: an unknown id or a cycle is an error naming both ends, never a silent skip; `ao board ready` prints exactly the set whose dependencies are all done and which wait on no human, and READY is always derived — a hand-written READY section is refused; the board file stays human-readable and human-edited, only its semantics become checkable; a test with a three-item chain, a cycle and a human-blocked leaf proves each case |
| 34 | **Size is a tripwire that asks a question, not a gate that reshapes the work** | the cap was invented on 2026-09-07 after a 20-file / 5,592-line candidate was killed twice at the review timeout, and it earned its keep: B8 (27 paths) took twelve rounds, while a 421-line candidate was reviewed in 11m48s and a 400-line one in 11m08s. Then it started costing: on 2026-09-08 a candidate whose behavioural fixes were already green measured 464 lines and the implementer began rewriting working, verified code to reach 400 — churn that invalidates the verification it already had, to satisfy a number. A single total also conflates 400 lines of fixtures with 400 lines of concurrency, and pushes coherent changes into two slices whose *interaction* nobody reviews — which is exactly how main went red the same day | Size is measured and reported by kind (product, tests, fixtures, generated, pure deletion) and by path count, never as one number; the guideline (≤5 paths, ≤400 product lines) is a tripwire: over it, the boundary must state why the slice is one invariant that cannot be split without leaving a seam unreviewed, and that statement goes to the reviewer; a refusal before spawning happens only far above the guideline, where a review is not credible at any length; **code that has passed its gates is never reshaped to satisfy a size number** — `ao` says so when the overshoot is small and the tree is green; the guideline relaxes once sectioned review (#26) lands, since the timeout that motivated it is gone; a test covers a green overshoot (advice, no refusal), an incoherent oversized candidate (refusal), and the by-kind report |
| 35 | **A slice's path boundary is proven possible before work starts** | 2026-09-07: B8a's boundary listed eight paths, but its own acceptance (K5, a durable delivery binding) could only be satisfied by touching `github-app-identity/model.ts`, which the boundary excluded; the implementer discovered this mid-slice, wrote a decision request and lost three hours waiting for an answer | At registration ao checks the declared boundary against the repository: every named path exists or is explicitly declared new, and every file that the acceptance text names or that plainly owns a named symbol is inside the boundary — anything outside is reported as a boundary conflict at creation, when widening or splitting is cheap; the check is advisory prose-matching, so it warns and names what it found rather than blocking, and a test with an acceptance naming a file outside the list proves the warning fires |
| 38 | **Review artefacts are retained by reference, pruned by age** | Voltrai holds 155 review artefacts (1.4 MB) of which 24 are tracked; they are the evidence a grant rested on, yet 131 of them exist only on one disk and nothing says which may be deleted | An artefact referenced by a board entry, a grant or a ledger row is retained indefinitely and is tracked in git; everything else is pruned by `ao prune` after a configured age, with a dry run by default and a summary of what was kept and why; `ao doctor` reports untracked artefacts that a grant depends on, and a test proves a referenced artefact survives a prune that removes its neighbours |
| 39 | **Merging is gated on a green run of the merge result** | 2026-09-07: four individually green branches were merged and main went red — PR #3 changed `architect_present`'s signature while PR #6 stubbed the old one; hosted CI never runs on push or PR by policy, so nothing caught it but a manual suite run afterwards | Before a merge, the suite runs on the merge result (main + branch, locally) and the exit code and summary are recorded as the merge's evidence; a merge without that evidence is reported by `ao doctor` as unverified, and the recorded run names both parents so a stale run cannot be reused; hosted CI stays manual-dispatch only — the gate is local and costs no Actions minutes |
| 42 | **A worktree lives exactly as long as its slice** | seven `agent-orchestrator-*` worktrees exist on this machine, one per slice, and none is removed when its branch lands; each is a full checkout with its own `node_modules`-scale footprint, its own `.ao/` state and its own stale review artefacts, and a merged one still answers `git worktree list` as if it were live | Registering a slice records the worktree it owns; landing or abandoning it prunes that worktree and its branch after checking the branch is merged or explicitly abandoned, and `git worktree prune` is run so no administrative file is left behind; `ao doctor` reports worktrees whose branch is merged or gone, with their size on disk; nothing is ever removed while its slice has an in-flight review or uncommitted work — a test covers merged, abandoned, dirty and review-in-flight |
| 43 | **Recall across projects, before the same price is paid twice** | ao already keeps everything worth remembering — chained ledgers, `D-*.json` decisions, review artefacts with embedded evidence, a 509-line `lessons.md` — and nothing reads it back: on 2026-09-07 the architect answered four near-identical path-boundary decisions in one day, and a defect described in `lessons.md` was rediscovered by measurement; `digest` and `since` summarise one project's recent activity, there is no search and nothing crosses projects | `ao recall <text>` searches decisions, lessons, review findings and waivers across every registered project and prints matches with their project, date and outcome; registering a slice or opening a decision runs the same search and surfaces near matches ("this boundary conflict was decided as D-… on …") before the question reaches a human; retrieval is plain text over the existing files with no index service, no dependency and no network; a result names the file and row it came from, so an answer can always be checked against its source |
| 44 | **Split `lib.py` and `cli.py` as pure moves, proven mechanically** | two files hold two thirds of the code (3,824 and 3,812 lines) and almost every slice touches one of them, so two lanes cannot run without meeting in the same file — which is exactly how main went red on 2026-09-07; `lib.py` is not one knot but 165 flat top-level definitions, so the split is regrouping, not untangling | The split happens only after behaviour is pinned (#41 golden tables, #7 attacks, #11 fuzzing) and after sectioned review (#26), and it lands as **pure moves**: no function body changes in the same slice as its move. The gate is mechanical, not a semantic review of thousands of lines — a check proves every moved definition is byte-identical to its previous body, the set of names importable from `ao` is unchanged, and the suite passes without edits; only the new seams (what became public across a module boundary) get a semantic review, and that diff is small. Seams follow the clusters this month's work exposed — review/candidate, authority/ledger, mail/protocol, watchdog/alarms, board/slices, process/presence — never a line-count target; one module per slice, and a slice that changes behaviour while moving is refused |
| 45 | **A standing bug-hunter, read-only and bounded** | [`roles.md`](roles.md) has defined a `bug-hunter` role — finds defects with no slice assigned, a different model family, a read lane needing no worktree — since before any of this was built, and nothing implements it (`ao` has no `role` or `lane` command). Meanwhile a cheaper model family sits idle: not good enough to hold a verdict, but well suited to producing leads. The hazard is known from the same day: a channel that fires constantly gets ignored, which is how three false red e-mails arrived in one minute | The hunter runs on a schedule, never continuously, over a bounded slice of the tree per run; it is a read lane — no worktree, no staging, no write to the repository, and it can never hold or influence a grant. Findings are **leads, not verdicts**: they land in the mailbox for the architect to triage into backlog rows or discard, and they never block a slice or raise a human alarm. Each finding carries a fingerprint (file, symbol, category) so a repeat is suppressed rather than re-sent, findings per run are capped, and a lead the architect discards is remembered as discarded. `ao cost` attributes the hunter separately so its value can be judged against its spend, and it is behind a feature switch that is off by default |
| 46 | **The governance survives the disk** | on 2026-09-07 the whole control plane of a project — `authority.md`, the board, the backlog, every `D-*.json` — was untracked in git and existed on one laptop; the code was safe on a remote, the decisions that authorised it were not. The architect had to freeze the work by hand into two `refs/wip/*` objects to make it durable at all | `ao backup` writes the project's governance (config, authority, board, backlog, decisions, ledgers, and the artefacts a grant depends on) to a destination the project names — a git ref, a second remote, or a directory — and records what it covered; `ao doctor` reports how old the last backup is and what is not covered by one; `ao restore` reconstructs the control plane from it and reports what it could not verify, and a test restores into an empty checkout and proves the chain and the board still validate |
| 47 | **State is checked against itself, and repaired on the record** | the board, the ledgers, the review artefacts and git refs are four stores that must agree, and nothing checks that they do: a grant can name a review file that was pruned, a board row can cite a commit that never landed, a ledger can outlive the worktree it describes. On 2026-09-07 the architect hand-edited three state files (a stale watchdog block, a cross-account credit ledger, an unchained authority ledger) with no way for the tool to confirm the result was coherent | `ao doctor --consistency` cross-checks the four stores and names every disagreement with both sides; `--repair` fixes only the mechanical ones (a missing archive pointer, an orphaned lock, a ledger whose worktree is gone), refuses anything that would change what was authorised, and writes what it did to the ledger so a repair is itself evidence; a test seeds each disagreement and proves it is found, and that a repair never rewrites an authority row |
| 48 | **Evidence is scanned before it becomes durable** | on 2026-09-08 a boundary asked for a reviewer's raw output to be kept as evidence and the decision had to reverse it, because review artefacts are committed to git and a model's stdout can carry credentials, tokens or prompt text. That was settled for one field; the class is not covered anywhere — mail, reports, decision context and verification output all land in the repository the same way | Anything ao writes that will be committed — review artefacts, mail, decision records, verification output — passes a credential-shaped-string check first; a hit is redacted with a marker naming what was removed, and the check runs before the write, not after; the patterns and their false positives are documented beside the check, and a test writes a token-shaped string through each of the four paths and proves none of them lands |
| 49 | **The process is measured by its outcomes, not by opinion** | on 2026-09-07 the round budget, the size guideline and the review pipeline were all re-decided in one day, on anecdote: the only figures available were the implementer's own estimate of where its hours went. `ao cost` measures spend; nothing measures whether a slice worked | Each slice records its outcome — rounds used, wall-clock from ready to landed, reviewer verdicts in order, size by kind, and whether a defect was later found in what it landed; `ao stats` reports the distribution across slices and projects so a process change can be judged against the slices before and after it; the record is written by the tool from what it already observes, never typed by an agent, and a test proves a slice that was re-specified keeps its history rather than starting a new one. Once #80 lands, the coordination figures come from the same place and cost nothing extra to collect: time to first `seen`, time to `handled`, age by class, and the one that would have caught every stall this month — messages with no `seen` at all |
| 50 | **Every store is bounded, and the bound is enforced without being asked** | measured 2026-09-08: `notices.jsonl` 3.2 MB, `nudge-*.log` 2.6 MB, `watchdog-*.log` 940 KB, `cycles-*.jsonl` 876 KB, and the implementer transcript ao reads for progress and cost is **189 MB**. `ao prune` exists, takes `--days`, defaults to a dry run — and nobody has ever run it, which is the only outcome a manual cleanup ever has | Each store declares its own retention by kind, enforced on write, not on request: pure observation (notices, cycles, nudge and watchdog logs, progress) keeps the most recent N records or K bytes and drops the oldest; evidence a grant depends on (verifications, artefacts a board row cites) is kept by reference as in #38; **the authority chain is never truncated by deletion** — retiring a prefix seals it, records the digest of the last retired row as the new genesis, and `commit-check` still validates across the seal, so a pruned ledger and an unpruned one give the same answer. `ao doctor` reports any store over its bound; a test proves the chain still verifies across a seal, and that a referenced verification survives a prune that removes its neighbours |
| 51 | **A measurement is never taken through a filter** | the implementer's shell runs a token-reducing proxy that rewrites and compresses command output. On 2026-09-07 it reported a 5,844-line staged diff as 533 lines to the architect, who nearly used that number in a sizing decision; the same proxy has silently altered `grep`, `ps` and `find` results in this session | Commands whose output ao treats as evidence — candidate size and paths, `git status --porcelain`, `rev-parse`, `write-tree`, `hash-object`, gate output, and ao's own commands — run unfiltered, and ao says so in the record: a measurement carries how it was obtained. `ao doctor` detects an interposing shell filter and names which of these paths it would touch; a test runs the measurement path under a filter that mangles output and proves the recorded number is the true one |
| 52 | **The implementer's shell filter is installed deliberately, after #51** | the implementer runs 300–400 tool calls a turn and repeatedly reaches 75% context; a token-reducing proxy is installed for the architect and not for the implementer in either project, and it would plainly cut both cost and context pressure. It also silently rewrote a 5,844-line diff as 533 lines in this session, which is why it must follow #51 rather than precede it | After #51 lands, the filter is installed for the implementer in both projects with an exclusion list covering every measurement path #51 names, and `ao doctor` verifies the exclusions are in force rather than trusting the configuration; the before/after effect is recorded — tool calls, credits and context high-water per turn for a week either side — so the saving is a measured number and not a claim, and the install is reverted if measurement paths are found going through it |
| 71 | **Windows and PowerShell fail closed** (audit: `bin/ao.ps1` and encoding findings) | the PowerShell entry point and several text paths behave differently or silently degrade on Windows, where `cwd` is also unavailable — and a guarantee that cannot be checked there currently fails open | Every guarantee that cannot be enforced on a platform refuses rather than degrading, and says which platform and why; text is read and written with an explicit encoding everywhere; the Windows lane runs the same authority tests as the others, and the ones that cannot pass are skipped with a recorded reason rather than silently absent |
| 72 | **The architect actor is named for its role, not for a model** | mail, steering and the watchdog's own wake prompt address the architect by a model's name because `mail_names()` falls back to a hardcoded default when `architect.name` is unset — which it is. Renaming is safe today: both `to_architect()` and `urgent_messages()` already accept the role-shaped form | The implementer performs the rename as its own recorded slice, **after AO25a lands and before it starts the next one**: set `architect.name` to the role name in `.ao/config.json`, rename the steering file that carries the old name, update the references inside it and in `board.md`/`backlog.md`, and confirm with `ao mail send` that delivery still resolves; the code-level fix stays #31, and the wake prompt's literal glob is corrected there |
| 80 | **Nothing is deleted to prove it was handled** ([docs/messaging.md](messaging.md)) | delivery-by-deletion buys one property worth keeping — *a message still present has not been handled* — and pays for it by destroying the record: a handled message is gone, so nothing accumulates, `ao room search` and #43 have nothing to read, and every mailbox is trapped inside one repository (the implementer had to file an ao finding into Voltrai's mailbox because that is the only one it has). But deletion was never the guarantee. The guarantee is that **the unhandled queue cannot drift from the truth**, and deletion is only one way to get it — the way that costs the history | Messages are append-only and never deleted. Handling is its own append-only record naming who handled it and what they did, and **the unhandled queue is derived: a message with no handling record**. The invariant survives intact — nothing is "handled" until someone has recorded handling it, in the same durable store as the message — while the corpus, the search and multi-recipient delivery all become possible, and a room is no longer a second channel: it is a message addressed to everyone, with each reader's position derived the same way. Local disk is managed by **compaction, never deletion** — a body older than the configured window collapses to a stub carrying its digest and a pointer, and the full record stays in the store. `ao room search` reads across projects; the Obsidian wiki becomes a *derived view* produced periodically, never the source of truth; and an agent may compact its own local copy but may never remove a record, because the actor deciding what was handled must not also be able to erase the question. Tests cover a derived queue that matches deletion's behaviour exactly, a compacted stub still answering "unhandled", and an agent's attempt to remove a record failing closed |
| 83 | **The message store syncs to one private repository, never to the product's own remote** ([docs/messaging.md](messaging.md)) | #80 makes messages durable but still local, and #46 says governance must survive the machine — mail *is* governance: decisions, boundaries, refusals. The obvious design, a ref pushed to the project's own remote, is unsafe here and measurably so: **`hakkisagdic/agent-orchestrator` is public**, so that push would publish this project's entire coordination history. It would also keep the trap that motivated a room in the first place — mail confined to one repository, which is why an implementer once filed an agent-orchestrator finding into another project's mailbox | Two layers. Locally the store is a dedicated ref (`refs/ao/mail`), never a product branch and never pushed to the product's remote — offline, no configuration, the same trick as `refs/wip/*`. Syncing goes to **one private repository holding every project's mail** under `refs/mail/<project>`, which is what makes cross-project addressing and `ao room search` work by construction, keeps coordination out of clones of a public product, and lets retention follow mail's own policy. Three preconditions, none advisory: the target's visibility is **verified private at push time by asking the host, never trusted from configuration**, and a public or unknown remote is refused, named and logged; #48's credential scan runs on every record before it leaves; syncing is opt-in per project with the repository named in config. `ao doctor` reports a store ahead of its remote and a configured target it could not verify. The concentration risk is documented rather than implied — one repository now holds every project's decisions |
| 81 | **Asking the codebase a question is a capability, not a copy** | twelve audit agents each grepped the same 11,498 lines from scratch, and #43 wants the same thing for our own records — but ao cannot embed a code-intelligence engine without breaking `dependencies = []`, and should not reimplement one | Question-answering sits behind the same content seam as #14: `ao ask "<question>"` resolves a configured provider, invokes it as a subprocess, and returns its answer with the files and line ranges it rested on — never an answer without citations. With no provider configured the command says so and names what would satisfy it, rather than degrading into grep. The owner's own [ctxman](https://github.com/hakkisagdic/ctxman) (MIT, npm) is the first provider — its method-level extraction and multi-language filtering are exactly the input such a question needs — and plansmith's `context-*` plugin split is the reference for how the capability decomposes. No dependency: absent the provider, the feature is absent and says so |
| 78 | **A review is a set of declared lenses, not one general look** | measured here on 2026-09-08: twelve finder lenses over this repository produced 110 distinct defect sites, and **91% of them were found by exactly one lens** — only ten sites were reached by two or more. Watchdog decisions, clock and quota arithmetic, concurrency, portability, secret leakage and subprocess surface each found things no other lens saw. A single general review is not a smaller version of that; it is one lens, and the other eleven findings are simply missed. The lenses that worked were failure modes, not job titles — \"developer\" and \"analyst\" have no distinct failure mode, \"concurrency\" and \"clock arithmetic\" do | A slice declares the lenses its review must pass, drawn from a named catalogue with a written question each one asks; the reviewer runs them as separate bounded questions and the verdict is per lens, so a clean correctness pass no longer implies a clean concurrency pass and the artefact says which lenses were actually asked. Default lenses come from what the slice touches — a slice with a timestamp gets clock, one that writes a file gets durability, one that spawns a process gets subprocess — and the architect may add or waive one on the record. This composes with #26: a lens is a section, so the cost is bounded by sectioning rather than multiplied by it. `ao review` reports coverage, and a landed slice whose lenses were never asked is visible rather than assumed |\n| 79 | **The role table is real before it grows** | `docs/roles.md` has defined seven roles — architect, implementer, reviewer, tester, bug-hunter, verifier, documenter — since before any of this was built, with a `roles.yml`, presets and `ao role set` in its examples. None of it exists: there is no `role` or `lane` command, and the only roles the code knows are architect, implementer and reviewer, resolved from `.ao/config.json`. Adding more role *names* to a table nothing reads is drawing more boxes; the mechanism has to come first | Roles are configuration the code actually resolves: an actor table and a role→actor assignment, `ao role` to show and reassign, the separation-of-duties invariant enforced on assignment rather than described, and a reassignment taking effect on the next slice while work in flight keeps its actor. Only then do new roles earn their place, and each must pass the same test as a review lens: name the failure it catches that no existing role catches. The first that clearly does is **analyst** — goal to dependency-ordered slices, this project's measured weakest surface (#33–#35) and the one Traycer is recorded as the reference for |\n| 76 | **The core knows about adapters, never about a particular harness** | `src/ao/adapters/` already holds seventeen harnesses — aider, amazon-q, amp, antigravity, claude-code, codex, command-code, copilot, cursor-agent, deepseek, gemini, kiro, ollama, opencode, qoder and two generics — which is the whole point: this tool is not written between two products, it has to work between whichever pair a user runs. But the implementation leaks: 103 references to specific harnesses sit in core modules (lib.py 36, cli.py 35, skillkit.py 26, watchdog.py 5, procs.py 1) — `detect` hunts for a `.kiro` directory and a named binary, the steering writer branches on the harness to pick a path, discovery hardcodes which store to read, process matching carries a name→binary table. Every one of those is a place a new harness fails silently rather than being unsupported honestly | Harness knowledge lives in the adapter definition and nowhere else: detection globs, config and steering paths, transcript shape, session store, process names, and the argv template are all fields an adapter declares; core reads them and branches on capability, never on identity. A harness with no adapter is reported as unsupported with what is missing, never guessed at. The invariant is testable and stays testable: a guard test fails when a harness identifier appears in a core module outside the adapter loader — the same shape as #31's guard, and the reason both exist |\n| 77 | **Anyone can ship an adapter without forking** | adapters are files inside the installed package, so supporting a new harness means editing ao's own source and waiting for a release — which is backwards for the part of the system most likely to change, and makes every new tool the maintainer's problem rather than its author's | Adapters load from outside the package too — a user directory and a project directory, both discovered, with package adapters as the base layer and later ones overriding by id; `ao adapters list` shows each one with its source and contract version, `ao adapters validate <file>` reports what a candidate is missing before it is used, and an adapter declaring a contract version this ao does not implement is refused with both versions named rather than half-loaded. A published conformance suite runs an adapter through the five capabilities against a fixture harness, so its author can prove it works and a regression in ao that breaks third-party adapters fails our own tests. `docs/adapters.md` gains the walkthrough for writing one, and the shipped seventeen become the examples rather than the privileged set |\n| 89 | **The adapter set covers the harnesses people actually run, and says which ones were verified** | the owner wants every provider a comparable tool supports. [Traycer's canonical vendor enum](https://github.com/traycerai/traycer/blob/main/protocol/src/host/agent/shared.ts) names twenty-one: claude, codex, opencode, traycer, cursor, grok, qwen, kiro, droid, kimi, copilot, kilocode, openrouter, amp, devin, pi, hermes, omp, huggingface, reasonix, antigravity. We ship seventeen and overlap on eight, so roughly a dozen are missing. **Its flags cannot be copied from there:** the open repository holds the protocol, the CLI client and the GUI, and the per-vendor launch arguments live in a Host binary that is not in it — checked 2026-09-14 (`--model` appears only in the CLI client's own options, and plansmith's `handoff-cli-generic` is a generic spawner that takes its argv from configuration). Only three of these are installed on this machine, so most of what we write cannot be tested here | One adapter per vendor on the target list, each written from that tool's own documented interface and carrying an honest `verified`: `full` only where it was exercised against an install, `untested` otherwise with the standing disclaimer — the same discipline `codex.json` already follows. Every adapter answers the capability questions of #88, including `trust_none` or an explicit statement that it cannot deny tools (which makes it reviewer-ineligible, as `trae` now demonstrates). One structural borrowing from Traycer is worth taking: a **canonical vendor list that every surface derives from**, so adding a vendor to one place without the canonical list is an error rather than a silent gap. `ao adapters list` shows each one's verification state, and `ao doctor` flags a configured adapter whose binary is absent |
| 88 | **A reviewer invocation is built from the adapter, so any pair of harnesses can hold the two roles** | the adapter definitions are good and already cover fifteen harnesses — `send.argv`, `options.model`, `options.effort`, transcript shape, busy detection, steering paths — but **only one of them (`kiro`) declares how to run with no tools**. Everything else declares `trust_all` and nothing for its opposite. A reviewer must run tool-less, so every reviewer in this project is a hand-written argv: `claude … --allowedTools ""`, then `kiro-cli … --trust-tools=`. That is why the pairing reads as one vendor against another rather than as two roles, and why moving to a different pair means writing argv by hand again instead of naming two adapters | Every adapter declares `trust_none` — the flags that deny tools, or an explicit statement that the harness cannot be run tool-less, which makes it **ineligible for the reviewer role** rather than silently unsafe. `ao role set reviewer <adapter> --model <m>` then composes the invocation from `send.argv` + `options.model` + `options.effort` + `trust_none`, so a pair is chosen by naming two adapters and a model each; a hand-written argv stays possible and becomes the exception, not the norm. Acceptance: the same project runs its two roles as at least three different harness pairs with no code change and no hand-written argv, `ao doctor` reports an adapter that cannot deny tools as reviewer-ineligible, and a test composes an invocation for every shipped adapter and asserts none of them grants a tool |
| 86 | **The reviewer is a tool ao invokes, not a session that shares the architect's quota** | two failures that look different are the same one. `shared-pool`: the reviewer and the architect run the same binary on one quota window, so when the architect's week ends the reviewer ends with it — which is exactly what happened between 2026-09-10 and 09-14, with every wake failing on `hit your weekly limit` and fourteen of them piling into `deferred.jsonl`. And provenance: a stand-in session produced a clean APPROVED for AO53 that ao **cannot legitimately record**, because `commit-ok` accepts an artefact ao produced or a human waiver, and nothing else. The second is not a gap in the stand-in idea — it is the correct refusal. What is missing is a reviewer ao can *invoke* that does not spend the architect's window | A reviewer adapter that ao runs itself, over a **local staged candidate** rather than a hosted pull request, with its provider chosen independently of the architect's — so the evidence is ao-generated (provenance intact) and the quota is someone else's (`shared-pool` gone). [pr-agent](https://github.com/The-PR-Agent/pr-agent) (MIT, pip, reaches OpenAI, Anthropic, Gemini, DeepSeek, Mistral and anything behind LiteLLM) is the first candidate and belongs in tier two: an optional extra that reports itself unavailable when absent. It must be proven on a local diff before it is adopted — its documented entry point is a PR URL, and a reviewer that needs a PR is no use to a staged candidate. Acceptance: `ao review` runs it against the exact staged candidate, the artefact carries the adapter and the resolved model, the architect's window is untouched, and a test proves the candidate digest in the evidence matches what was staged |
| 84 | **The implementer has a written envelope for working without an architect** | the architect is a person's quota and a person's attention, and both run out: on 2026-09-09 the architect's weekly limit ended a session with the queue full and the implementer mid-slice, and the same thing happens whenever the owner is asleep or away for a weekend. Today nothing states what the implementer may settle alone, so it either stops on every question — the queue is long and stopping is the expensive answer — or it decides and hopes. #8 keeps it from idling; it does not say what it may decide | An **autonomy envelope** written where the implementer already reads: what it settles alone (which queued item comes next, how to split a slice that will not fit, whether a finding is in scope, when to park and move on), what it may never settle regardless of how long nobody answers (landing without an independent review, widening its own authority, anything `authority.md` forbids), and what it does when blocked — record the question, park with the reason, take the next READY item, and never let an unanswered question stop the queue. Parked questions accumulate as a list the architect answers in one pass on return, rather than as a stalled agent. `ao doctor` reports how long the architect has been absent and how many questions are waiting; a test proves a blocked slice parks and the next item starts |
| 85 | **A stranger can get from clone to first slice** | everything here is written for a project that already runs: `ao init` exists, `docs/` is thorough, and yet nothing walks someone from an empty checkout to a first reviewed commit, and the parts most likely to defeat them are the ones this month proved fragile — a hook installed where Git will not run it (#53), a reviewer that must be a different actor and quietly is not (#60), a config whose one wrong field silently drops authority (#56). Release 0.5.0 (#12) publishes a tool nobody has been shown how to start | A first-run path that ends in a **proven** state, not a printed one: `ao init` finishes by running the guarantees rather than describing them — the hook is executed and must refuse a synthetic candidate, the reviewer is resolved and must be a different actor than the implementer, a throwaway slice goes through review and commit-ok end to end — and each check reports what would fix it when it fails. `docs/` gains one short getting-started page that is the same sequence a person types. A test drives the whole path in a temporary repository, so the walkthrough cannot rot |
| 73 | **A boundary too big for one sentence moves to a file, and the row points at it** | the acceptance boundary lives as prose inside a backlog row, and on 2026-09-07/08 the boundary itself was repeatedly the thing that was wrong: AO25b's asked for a reviewer's raw streams to be kept as evidence, which contradicts the secret-safety the code was already enforcing; B8a's named eight paths while its own acceptance could only be met by touching a ninth. Both were found mid-slice, by an implementer that had already started. Neither was diffable, because a boundary lives in a table cell and changes leave no trace. Kiro and Traycer both already write this shape — specs and requirements as markdown beside the code | Above a stated size, a slice's boundary is a file in the repository (invariant, numbered scenarios, declared paths, explicitly out of scope) and the backlog row carries a pointer to it and nothing else — **one source of truth, never two**; the reviewer judges the candidate against that file at the commit it names, so a boundary that changed mid-slice is visible as a diff; #35's feasibility check runs against the file's declared paths at registration; a slice small enough for one sentence keeps the sentence, because ceremony was measured at 68% of spend and this must not add a second artefact to keep in sync |
| 92 | **Configuration present is not capability available — and a human notice is never held for an actor whose reachability was assumed** | the owner's words, 2026-09-14: *nobody fixed anything and nobody told me until I asked*. They were right, and `ao notices --all` names the reason. From 19:11 to 19:18, every two minutes: `held  Voltrai: anomaly  decision-requested — architect will be woken`. **Held, not sent.** The implementer had parked at 19:13 on an expired reviewer token, saying in its own words that this required human input, and the human notice was suppressed in favour of an architect wake that never happened. `watchdog.py:481` decides that reach from `(cfg.get("architect") or {}).get("argv")` — whether a config key is non-empty — not from whether the architect can be run. The key holds a binary name, so the string says *will be woken* even when that actor is out of quota, its wake is deferred (#87), or it is a session that only exists while a person is typing. A comment two lines above records that an earlier version overstated its reach and was fixed; the fix checked configuration and stopped short of reachability. Same shape found twice more the same day: the pre-commit hook treating a directory's existence as proof the repository is under ao (#90), and `ao status` treating a branch's own commits as proof a checkout is current (#89). Three surfaces, one error: **the presence of configuration read as evidence of capability.** | Any condition the implementer itself declares as requiring human input reaches a human channel directly and is never held for an agent, which needs a declared blocker class to read (#8's `waiting:` field). Holding a notice only for an architect that will actually be woken is closed as #92a. |

**The 2026-09-08 adversarial audit** ([`docs/audit/2026-09-08-adversarial.md`](audit/2026-09-08-adversarial.md)) ran to completion: 502 agents, three rounds, 155 findings reported, **119 confirmed — 16 BLOCKER, 77 HIGH, 26 MEDIUM**, plus three more BLOCKERs raised by the completeness critic. #53–#64 are the ones that break a stated guarantee outright; #65–#71 carry the remaining 103 findings as seven themes, each named by the guarantee it protects — a slice is done when every finding the audit filed in its area is closed or explicitly declined in the report, so nothing is quietly dropped. The implementer's own attack suite (#7) independently reached five of the same defects — index race, forged review provenance, waiver replay, hook removal, ledger tail truncation — and its red tests in `kiro/authority-attacks` are the acceptance evidence for the slices that fix them.

**Queue order is not row order.** Rows are numbered as they were written; what runs next is
whatever the project board says. As of 2026-09-09 the order is:

1. **The guard is off entirely** — #53, #59, #58. A hook Git never runs, a status that says it
   does, and a shipped allowlist containing its own bypass. Nothing else matters while these hold.
2. **The guard can be fooled** — #54, #60, #62, #61, #56, #63, #57, #64. Ordered so #62 lands
   early: the truncation hole is the one the ledger was supposed to close, and the
   [Merkle checkpoint](upstream.md) design closes it with no dependency.
3. **Do not stop when nobody answers** — #75 first, ahead of the rest of tier 2 if a stand-in is
   already being opened by hand ([docs/standin.md](standin.md) is that path today), then #74,
   because the stand-in flow needs settings to exist.
4. **Find what we have not thought of** — #7 and #11 (Hypothesis as its own optional extra;
   the lane skips with a recorded reason when it is absent), then #41's golden tables. Moved up on 2026-09-07's evidence and confirmed on 09-08: twelve lenses found 110
   sites, 91% of them seen by exactly one lens.
5. **Stop paying the same costs** — #26, #27, #28 (the review pipeline), then #51 and #52 (the
   measurement filter, in that order), #42, #29, #30, #37, #36, #40, #20, #19.
5b. **Make the pair interchangeable** — #88, then #76 and #79. Raised out of tier six on
   2026-09-14: the roles are currently held by one particular pair of harnesses because a reviewer
   invocation cannot be composed from an adapter, and the owner intends to run other pairs. This is
   a product property, not a preference — a tool that orchestrates *any* two agents must not read
   as written between two.
6. **Make the shape right** — #33 (`graphlib`, in the standard library on our floor), #34, #35,
   #73, #78, #79, #38, #39, #43, #80, #81, #46, #47, #48 (gitleaks' rules), #49, #76, #77, #44.

The five borrowings in [upstream.md](upstream.md) are placed by the gap they close, not adopted
as a batch: `graphlib` with #33, the Merkle checkpoint with #62, gitleaks' rules with #48,
Hypothesis with #11, in-toto's statement shape with #61. **None of them touches the core.** Three
are designs, formats or rule sets written against the standard library; `graphlib` *is* the
standard library; Hypothesis is an optional extra whose lane skips when it is absent. The rule
`dependencies = []` is about the core only — see the three tiers in [upstream.md](upstream.md) —
and a tier-two or tier-three feature earns its package by failing closed and saying so.

**Operational debt held against #40 — and a correction.** On 2026-09-07 the Voltrai doctor job
was unloaded (`launchctl bootout gui/501/com.agentorchestrator.doctor.voltrai`) because every
finding it produced went to the human channels every fifteen minutes, and three false red e-mails
arrived in one minute. **That unload did not hold.** Measured 2026-09-14: `launchctl list` shows
`com.agentorchestrator.doctor.voltrai` loaded again, last exit 1. A plist left in
`~/Library/LaunchAgents/` is re-bootstrapped by launchd on the next login, so `bootout` alone is a
pause, not a stop — and a human who believes a job is off while it is on will misread every alert
it sends. That is why the notifications kept arriving after the fix: the job never stopped running
`ao doctor --check` on a fifteen-minute timer, and `--check` pages a person by design.

The real correction is #40 (a command a person or an agent runs never pages anyone), and the
recorded operational fact is now the opposite of what this paragraph used to say: nothing needs
re-loading. If it must genuinely be stopped before #40, the plist has to be disabled
(`launchctl disable gui/501/com.agentorchestrator.doctor.voltrai`) or moved out of
`~/Library/LaunchAgents/`; the undo is:

```
launchctl enable gui/501/com.agentorchestrator.doctor.voltrai
launchctl bootstrap gui/501 ~/Library/LaunchAgents/com.agentorchestrator.doctor.voltrai.plist
```

Landing #40 without confirming which of the two states the job is actually in leaves the fix
unproven.

Two policies stay outside the queue: hosted CI runs only on the maintainer's word, one
environment at a time; nothing in this repository lands without an independent review
of the exact candidate.

**Closed since 2026-09-15.** Rows leave the table when they land; this list keeps their
numbers accounted for and points at the evidence.

| # | Slice | Closed by | Evidence |
|---|---|---|---|
| 94 | A checkout's local state is gitignored before it can be committed, not after | `a3f223f` (review deferred by the owner, waiver W-1789484981) | `git check-ignore` reports `.ao/`, `.claude/`, `.kiro/`, `.codex/` and `semantic-review/` ignored while `.ao-project` stays tracked. The `ao init` clause was dropped: in product repositories `.ao/` holds evidence and stays tracked; only this public repository keeps its own development state local. |
| 99 | Nothing derived from subprocess output is written inside the repository | AO59b (`6cb2957`) | persisted reviewer reasons carry only closed values ao produced; `tests/test_review_chain.py` asserts a secret on reviewer stderr reaches the operator's terminal and never the reviewer state. |
| 103 | A project set up before the marker stays governed by its config | AO59e (review deferred by the owner, waiver W-1789486370) | measured 2026-09-15 before reinstalling the tool: under the AO59a rules Voltrai - a real ao project whose hook is `exec ao -C … commit-check` and which has never tracked `.ao-project` - read as `uninitialized`, and `commit-check` returns 0 for that state, so the upgrade would have turned its commit gate into a silent pass. `_project_enrollment` now returns `legacy` when `.ao/config.json` exists, no canonical marker is in the index or HEAD, and no commit in HEAD's history ever touched the marker; legacy is enforced and an unreadable config is broken, while a completed removal and an incidental `.ao/` without a config stay unenforced. `ao hooks install` refuses in a legacy project, and doctor advises adopting the marker first. Six tests in `tests/test_hook_paths.py`. |
| 104 | A retrospective review covers only its own slice's commits | CATCHUP-RANGES (review deferred by the owner, waiver W-1789486750) | measured 2026-09-15 while planning the implementer's return: `ao catchup` reviewed `head..HEAD` for every open review waiver, so with one waiver per slice the earliest waiver's review carried every later slice under the first slice's boundary, and N slices became N overlapping reviews. Each waiver now reviews from its head to the head of the next review waiver, open or closed, and the newest runs to HEAD; a waiver that covered no commits is closed, and the newest stays open until something lands. Recorded heads must be full object ids and git runs without a shell - the old loop interpolated a ledger value into a shell command. Catchup no longer runs a watchdog cycle for a project with no watchdog: the cycle wrote a heartbeat that went stale and read as a dead watchdog to every sibling. Five tests in `tests/test_switches_and_bypass.py`. |
| 105 | The newest review is a review, not a placeholder | REVIEW-LISTING (review deferred by the owner, waiver W-1789487298) | measured 2026-09-15: every `ao verify` in a freshly initialised project printed `newest review: INVALID (.gitkeep)` and wrote `.gitkeep` into the verification record as the review the tree was measured against, because `A.reviews` listed every entry in the reviews directory by modification time and `ao init` leaves a placeholder there. Only regular files whose names do not start with a dot are listed now. One test in `tests/test_switches_and_bypass.py`. |
| 89 | A checkout's distance from the tip is part of every measurement it produces | GIT-POSITION (review deferred by the owner, waiver W-1789489060) | measured 2026-09-14 by getting it wrong: `ao status` printed "0 commits unpushed" for a branch 77 commits behind main and already merged into it, because `git_state` counted `origin/main..HEAD` only and turned any git failure into "0". `git_state` now measures against the remote default branch (`origin/HEAD`, then `origin/main`, then `origin/master`), reports behind as well as ahead with one left-right count run without a shell, flags a branch that is already merged (never the default branch itself, which reads as behind), falls back to `origin/main` when `origin/HEAD` names a deleted branch, and reports "?" with no base instead of "0". `ao status`, `ao doctor`, the handoff note and the MCP status payload carry the position. Six tests in `tests/test_git_position.py`. |
| 97 | A review request distinguishes the candidate from the context it is judged against | REVIEW-REQUEST (review deferred by the owner, waiver W-1789490034) | The request names the candidate, the diff that may land, and separately a read-only context. When every Python file in a candidate is a test, `review_context` reads the definitions those tests reference through their imports from the committed tree - the tests from the index tree, the source from HEAD - and attaches them whole, in first-reference order, up to a byte budget, naming what does not fit; the prompt travels as one argument, so context only takes the room left under 120 KB and never tips a prompt past Linux's single-argument limit. Context enters neither the diff nor the candidate digest; a retrospective range takes it from its end commit, and the review file records it. `tests/test_review_context.py` includes a one-path test-only candidate reaching a verdict with no context named by a human. |
| 98 | A reviewer needs a way to say "outside the candidate", or it says it with a severity | REVIEW-REQUEST (review deferred by the owner, waiver W-1789490034) | The request opens with the decision rule on its own lines and gives a Notes section with no severity that is not counted and cannot change the verdict, telling the reviewer that an out-of-scope concern belongs there rather than at a raised severity. A test keeps a reviewer's notes, one of them tagged HIGH, in the review file with the verdict still APPROVED. |
| 96 | Idle is not free, and a stopped implementer should stop | IDLE-ANSWER (review deferred by the owner, waiver W-1789491106) | Two causes, both measured. The watchdog counted its own bookkeeping as work: `work_fingerprint` and the progress record included the ledgers it appends every cycle, so the fingerprint changed on every cycle, the nudge backoff reset before it could grow, and the spin check could never see a frozen run - the cycle log for 07:00 to 12:00 on 2026-09-15 shows 29 nudges and not one backoff. And a transcript write reset the backoff too, although a nudged turn that finds nothing to do writes its answer like any other. Separately, `product_dirty` read `git status` through `sh()`, which strips its output, so the first line lost its status column and its path a character and could read a coordination file as a product change; it now reads the status unstripped. Correction, measured after the fix was deployed: in Voltrai that misreading changed no decision - ten real product paths were uncommitted there, eight of them a staged candidate, so "uncommitted changes" was a true reason - and an earlier version of this row said the misreading caused it. Both measures now count only what an agent produces - HEAD, uncommitted product paths and their content, reviews, decisions and the ledgers agents write - and only that resets the backoff. When a nudged turn has ended, written its answer and changed nothing while nothing it is given moved, the watchdog enters an idle state once, tells the human once, and does not nudge until the board, the backlog, a decision, its mail or the work moves; `ao status` and the MCP status show since when. Detection reads behaviour, not the wording of a report; an unanswered nudge still backs off. Seven tests in `tests/test_idle_answer.py`. |
| 106 | One condition rings one alarm, whoever sees it | ALARM-DEDUPE (review deferred by the owner, waiver W-1789491689) | measured 2026-09-15: the exhausted credit pool stood as two alarms, `Voltrai:credits-exhaust` from the watchdog at red and `Voltrai:doctor:credits-exhaust` from the doctor check at orange - two ids, two levels and, once both ring red, two mails for one fact. They cannot share an id because every touch rewrites the level. The doctor check still prints every problem and exits 1, but for the conditions the watchdog raises itself - the credit pool running out and a failed architect wake - it rings only while the watchdog's own alarm is quiet, so a stopped watchdog still has its backstop. A reading over the limit also reads as exhausted: at 12503/10000 the doctor still said "runs out 15 Sep, before the reset" because it checked the projection first. Three tests in `tests/test_doctor_alarms.py`. |
| 91 | Throughput is candidates landed, not turns taken, and something reports it | THROUGHPUT (review deferred by the owner, waiver W-1789492266) | `ao status` has a throughput section for a window named with `--window` (hours, default 24): candidates staged (verified ready), candidates landed (a commit's tree is the candidate's index tree), decisions asked and still waiting with the oldest one's age, and a state read in order - stalled (the newest staged candidate unlanded for `stall_minutes`, default 60, with its reason from a refused commit, an unapproved review or the decision it waits on), landing, staging, busy (turns, nothing staged) and idle (no turns). It reads the ledgers, git and the transcript's last write, never the mailbox, and the MCP status carries the same figures. Five tests in `tests/test_throughput.py`. |
| 107 | A gap in the watchdog log says whether a cycle ran long or none ran | CYCLE-DURATION (review deferred by the owner, waiver W-1789492837) | measured 2026-09-15: the Voltrai cycle log had eighteen gaps of 5 to 27 minutes between 12:02 and 16:33 against a two-minute launchd interval, pmset recorded no sleep, and a row carrying only its end time could not say whether a long cycle held the next one back or none ran. Each recorded cycle now carries `started` and `seconds`, and `ao watchdog status` prints the longest recent cycle and the longest silence between one cycle's end and the next one's start. Older rows count by their end times. Two tests in `tests/test_cycle_duration.py`. |
| 108 | An alarm nobody can act on yet waits, on the record, until a date | ALARM-SNOOZE (review deferred by the owner, waiver W-1789495304) | measured 2026-09-15: the doctor check began reporting Voltrai's legacy commit hook, a true finding whose fix waits on the owner until 1 October; left alone it turns red within the hour and mails every six hours. `ao alarms snooze <key> --until YYYY-MM-DD --why '...'` keeps that one alarm off the human channels until the date: `notify` records the notice as held and does not touch the ladder. A snooze needs a future date and a reason, records who set it, is listed by `ao alarms`, lifts with `ao alarms unsnooze` and ends by itself. Three tests in `tests/test_alarm_snooze.py`. |
| 93 | Slow tests stay in the one suite while it runs in minutes | DOC-93-CLOSED (review deferred by the owner, waiver W-1789496154) | measured 2026-09-15 on an idle machine: the pre-push suite ran 397 tests in 204 s, and its slowest tests are hook-execution probes that spawn git, 7.5 s and 4.8 s. Runs of 473 to 765 s the same evening were the machine, not the code: the three slowest tests timed on 08000ab, whose full verify had taken 241 s, and on 3a69ee4 took the same (7.85 and 7.46 s, 4.71 and 4.82 s), a process spawn cost 25 ms for Python and 11 ms for git, and the next push was back to 204 s. The 24 s fixture setup behind the request never reappeared; the largest setup measured was 0.31 s. The owner chose to remove a cause before splitting anything; there was none in the code, so there is no second suite. |
| 100 | The review chain has a total budget, not only a per-route one | CHAIN-BUDGET (review deferred by the owner, waiver W-1789496557) | Discovery already had a shared 30 s deadline; the chain did not: every route ran with the full review timeout and every transient route ran again. One walk of `_invoke_reviewer_chain` now fits two full attempts, each with room for discovery and the kill drain, and the 30 s retry wait between them (`_review_chain_budget`). A route starts only while the deadline leaves that room and gets no more than what remains; routes the budget never reached, and transient routes it cannot retry, are recorded with a reason naming the budget, so the review closes as UNAVAILABLE. A single reviewer keeps its full timeout and its retry. `ao doctor` states the worst case: 31m 40s for a review, 4m 40s for the reviewer probe. Three tests in `tests/test_chain_budget.py`, one of them a chain of four never-finishing routes stopping inside its budget on a fake clock. |
| 101 | An OS error with no errno is closed on purpose, and the reason is written down | DOC-101-DECISION (review deferred by the owner, waiver W-1789497242) | Decision: keep the permanently-closed default for an OS error with no errno. Without an errno nothing tells a resource shortage from a fault that would repeat, and a retry would spend a reviewer call and part of the review's budget (#100) on a guess; closing costs one UNAVAILABLE review that can be repeated, the safe direction for a gate. `BlockingIOError` stays the one errno-less transient failure because its type names the resource condition. `docs/roles.md` now states this with its cost next to the rest of the failure taxonomy, so it is a recorded decision rather than an accident of the order of the checks; behaviour is unchanged and `test_unknown_os_error_without_errno_is_permanently_closed` still pins it. |
| 102 | The marker fingerprint is asserted against a replacement, not only against a deletion | MARKER-FORGERY (review deferred by the owner, waiver W-1789497330) | `tests/test_marker_fingerprint.py` writes the fields instead of waiting for a filesystem: a same-length replacement of `.ao-project` served with the original's exact device, inode, mode, size and nanosecond timestamps is still caught by its bytes, and `ao init` refuses a marker replaced that way during the reviewer probe without writing a config. The finding is that no field is missing: the fingerprint reads one byte past a valid marker, so it holds every byte of one, and a replacement it cannot tell apart is byte-identical. No code changed; three tests. |
| 40 | A standing advisory never becomes e-mail, a resolution is never louder than its raise, and a quota block ends with its window | QUIET-CHECK (#40a) and QUOTA-WINDOW (#40b), review deferred by the owner (waiver W-1789559543 for #40b) | #40a: `ao doctor --check` pages nobody; the scheduled job runs `--check --notify` and pages only red findings (dead watchdog, failed wake, blind transcript, exhausted credits); advisories are recorded for the architect; a red condition with a known end is mailed once and held until that end; an alarm that stops being raised is recorded as no longer raised, on the desktop only if it was red. #40b: a reset is read against the time the wake wrote it and never rolled past the five-hour window, a quota error ends its block at its reset or one window after it was written, and a present architect clears a cached block. Tests: `tests/test_quiet_check.py` (six) and `tests/test_quota_window.py` (three); `test_quota_failure_carries_reset_time` now checks the reset against the time its line was written. |
| 1 | Interactive architect presence suppresses wakes for the whole session | `dc548c3` (merged `7e1f3aa`); found done on main 2026-09-16, closing record waiver W-1789560207 | `architect_present` measures the live process tree and is checked before both architect spawn paths. Tests: `test_live_interactive_architect_suppresses_pending_report_wake`, `test_dead_architect_releases_refill_on_the_next_cycle`, `test_architect_presence_measures_a_live_interactive_configured_process`. |
| 2 | Rounds are scoped to a slice, not to a HEAD | `798e907` (merged `763b681`); found done on main 2026-09-16, closing record waiver W-1789560207 | `rounds()` counts only index-candidate evidence whose slice is the running board item. Test: `test_rounds_are_scoped_to_running_slice_at_the_same_head`. |
| 3 | Authority ledger tamper evidence | `a677110` (merged `8d51406`); found done on main 2026-09-16, closing record waiver W-1789560207 | Each authority row carries the digest of the previous one, validated on read and before every append. Test: `test_commit_check_refuses_a_broken_authority_chain_without_mutating_it`. Cutting rows off the end is not detected; that is #62, and `docs/ledger.md` says so. |
| 53 | Hooks are installed where Git actually looks | `cde8643` (merged `03fa6ec`); found done on main 2026-09-16, closing record waiver W-1789560207 | Hook paths come from `git rev-parse --git-path hooks` and the effective `core.hooksPath`. Tests: `test_relative_hooks_path_uses_linked_worktree_top_not_private_git_dir`, `test_unconfigured_linked_worktrees_share_common_hooks`, `test_bare_repository_with_attached_worktree_uses_common_hooks`. |
| 59 | Hook state is proven by execution, not by grepping a comment | `e81ac5c` (merged `0678788`); found done on main 2026-09-16, closing record waiver W-1789560207 | `_hook_execution_probe` runs `git hook run pre-commit` against a synthetic index and accepts only ao's nonce-bound refusal; status, doctor and init report the same probe. Tests: `test_marker_preserving_exit_zero_and_misplaced_body_are_not_installed`, `test_non_executable_current_body_is_not_installed`, `test_status_doctor_and_init_report_the_same_execution_proof`. |
| 58 | The implementer's own allowlist does not contain its bypass | ACTOR-GRANTS (review deferred by the owner, waiver W-1789560424) | `src/ao/allowlist.py` asks each permission rule what it would admit against a list no actor may be granted: skipping or redirecting the commit hook, a push window, a waiver, removing the hooks, `ao lock`, and the common interpreters. The Claude Code implementer grant names each ao command and no interpreter, and commits through the new `ao commit`, which runs the authority check itself before Git runs; the architect grant names its ao commands and drops `find`. `ao doctor` reports any configured actor whose grant admits one of these, or which is granted every tool. `tests/test_allowlist.py` enumerates every shipped adapter. A harness granted every tool, as Kiro is, is #109. |
| 62 | Truncating a ledger is detectable | LEDGER-LENGTH (review deferred by the owner, waiver W-1789561007) | Each chained row carries its ordinal, and every append records the ledger's committed length and last-row digest outside the repository (`~/.ao/ledger-checkpoints.json`, `AO_LEDGER_CHECKPOINTS` for tests). Reading refuses fewer rows than recorded, a missing file, or a recorded row whose digest differs, and nothing is appended onto a shortened ledger, so `commit-check` refuses it and names the cut. `tests/test_ledger_length.py` truncates at four points including to empty, deletes the file, and runs commit-check on a cut authority ledger. `docs/ledger.md` and `docs/safety.md` state the limits: a fresh machine has no record, a same-user process can rewrite it, and retiring a ledger means removing its entry by hand until #50 seals old rows instead. |
| 54 | A verdict that contradicts its own findings never reaches disk as APPROVED | REVIEW-VERBATIM (review deferred by the owner, waiver W-1789561635) | The artefact carries the adjudicated verdict and counts at the margin, an `- adjudicated:` line naming the counts (or the candidate change) whenever that verdict differs from the reviewer's, and the reviewer's output unchanged beneath `## Reviewer output, verbatim`, indented so no line of it is read as a field. `tests/test_review_artefact.py` files an APPROVED review with BLOCKER 2 and HIGH 1 as NEEDS_CHANGES and proves `commit-ok` refuses it. |
| 55 | The verdict is read from the verdict line, not from anything that mentions one | REVIEW-VERBATIM (review deferred by the owner, waiver W-1789561635) | Every header value — boundary, commit range, reviewer and implementer labels, context paths, reasons — is written as a JSON string unless printable, so no separator `splitlines` honours can start a line (`--paths` was already JSON). Tests inject LF, CR, VT, FS, NEL and U+2028 through the boundary of completed, unavailable and invalid artefacts; the golden table gains an escaped-boundary row. |
| 57 | A review is not classified unavailable because of what it talks about | REVIEW-VERBATIM (review deferred by the owner, waiver W-1789561635) | Unavailability comes from the reviewer's process (exit status, silence, timeout). The prose fallback in `reviews()` now needs no verdict line and no evidence line, so it reaches only legacy one-line quota artefacts. A review whose findings discuss authentication and rate limits counts as a round and, approved, authorises `commit-ok`. |
| 60 | Reviewer identity comes from what ao wrote, never from what the implementer supplied | REVIEWER-IDENTITY (review deferred by the owner, waiver W-1789562090) | A completed review's evidence records the adjudicated verdict, the counts and (without a matrix) the reviewer's id, family and fallback flag. `commit-ok` reads the reviewer from the evidence, refuses an approval that records none, and treats a reviewer as the implementer when its id is the implementer's resolved session or its command carries that session as an argument; `cmd_review` never runs such a route. `reviews()` reads a margin verdict that disagrees with the evidence as INVALID. `tests/test_reviewer_identity.py` covers a boundary naming another reviewer, a forged label that resumes the implementer, a session id inside another id, a missing reviewer and an edited verdict. |
| 56 | A config write cannot erase an authority opt-in | CONFIG-DURABLE (review deferred by the owner, waiver W-1789562783) | Every write of `.ao/config.json` (feature switches, profile blocks, `ao init`) goes through `storage.replace_file_durably`: temporary file, fsync, rename, directory fsync, so the file is seen whole, old or new. Writers read through the bounded config reader and refuse a config they cannot read rather than rebuilding it; `commit-check` already refused an enrolled project with an unreadable config. `tests/test_config_durability.py` kills each writer after writing, after syncing and after renaming and proves the config parses and keeps `capability_matrix`. |
| 61 | A gate result is bound to the gate definition that produced it | GATE-DEFINITIONS (review deferred by the owner, waiver W-1789563169) | A verification record carries the canonical digest of the definitions its profile ran and each gate's command; `commit-ok` and `commit-check` refuse when `.ao/gates.json` no longer matches. The verification ledger is chained like the authority ledger; unlinked rows from before the chain stay first as history and never count, and a row appended without a valid link makes the ledger unreadable. `tests/test_gate_definitions.py` rewrites `gates.json` between verify and commit-ok and after the grant, and appends forged rows with no link and a wrong one. |
| 64 | The grant binds the tree Git commits, not the tree the hook saw | LANDED-TREE (review deferred by the owner, waiver W-1789564309) | The grant records the index tree; `ao commit` compares HEAD's tree with it after Git commits and prints, notices and records a mismatch as an authority refusal. `ao doctor` reports commits since the first grant's base whose tree no grant bound. `docs/safety.md` states the limit beside the commit-check claim. `tests/test_landed_tree.py` reproduces the race with a pre-commit hook that stages a path after the check. |
| 63 | The newest review is the one that counts, and it cannot be starved | REVIEW-SELECTION (review deferred by the owner, waiver W-1789564735) | Every artefact `cmd_review` writes gets a unique name and a row in the chained `.ao/ledger/reviews.jsonl` (file, sha256 of its bytes, candidate, verdict, reviewer, fallback). `commit-ok` and `commit-check` take a candidate's reviews in ledger order: UNAVAILABLE masks nothing, the newest decides, a missing or rewritten artefact is a named refusal, and a fallback's approval cannot supersede a completed rejection. The timeout is `review_timeout` in config; `--timeout` is accepted and ignored. `tests/test_review_selection.py` covers an emptied, cut and deleted newest review, a touched older approval, a fallback after a rejection, and a starved primary. |
| 67 | The waiver surface is bounded | WAIVER-BOUND (review deferred by the owner, waiver W-1789565799) | `ao waive` requires one slice and a person (not an agent, a role or "human"), expires after 24 h by default (at most a week), and records login and terminal beside the name. `waivers.jsonl` is chained; opening and retiring are chained appends and a failed close raises. `commit-ok` takes only an unexpired waiver for a running slice that has authorised no other candidate and records it in the grant; `commit-check` holds the grant to it; `ao doctor` lists open waivers with age and expiry. `tests/test_waiver_bounds.py` covers the refusals, expiry, a replay for other bytes, a forged unlinked row and a close that cannot be written. |
| 17 | `catchup` retro review is bound to the waiver's landed commit | WAIVER-BOUND (review deferred by the owner, waiver W-1789565799) | A bounded waiver is reviewed on exactly the landed commit whose tree was granted under it; one whose granted tree never landed is UNRESOLVED; one never used closes when it expires. Legacy waivers keep their head-to-next-head range. A waiver closes only on the retrospective review recorded for exactly that range, or on a range with no net change; an unavailable, invalid or unrun review keeps it open. |
| 7 | Adversarial suite against commit authority | ATTACK-SUITE (review deferred by the owner, waiver W-1789565801) | `tests/test_attacks.py` names ten implementer attacks — stage swap, index race, forged review evidence, retrospective review as authority, waiver replay, coordination-path smuggling, hook-state removal, ledger truncation, rename, `GIT_INDEX_FILE` spoof — and each fails closed; the index race is detected (#64), not prevented, and the test says so. |
| 90 | A hook that cannot establish ao's state refuses; it does not skip | ATTACK-SUITE (review deferred by the owner, waiver W-1789565801) | The tracked `.ao-project` marker separates a non-ao clone from an ao project whose state is gone; `test_attack_07_hook_removal_fails_closed` deletes `.ao/` after a grant and proves commit-check refuses and `ao commit` lands nothing. |
| 109 | A harness granted every tool is held by `ao commit` and the commit hook, not by an allowlist | ATTACK-SUITE (review deferred by the owner, waiver W-1789565801) | The watchdog runs the landed-tree check every cycle and tells the architect once about new commits whose tree no grant bound; `ao doctor` states which guard holds for the configured implementer. `tests/test_commit_guard.py` commits around the hook and proves one notice, and checks the doctor sentence for Kiro and for a scoped Claude Code grant. |
| 70 | A gate's result is read structurally | GATE-SUMMARY (review deferred by the owner, waiver W-1789565802) | The exit code decides; `lib.gate_counts` reads counts only from the runner's closing summary in the last 30 lines (node TAP/spec, pytest, or a gate's own `summary` regex), the last one counting; an unfound summary is recorded as `counts: null` and fails a gate with `min_tests`. `tests/test_gate_counts.py` feeds a misleading earlier summary and proves the recorded result follows the exit code and the real summary. |
| 68 | A ledger append is all or nothing | LEDGER-ATOMIC (review deferred by the owner, waiver W-1789565803) | A row is committed by its newline: readers never return an unterminated final line and the next append truncates it. A failure at any append step (short write, fsync, directory fsync, length record) truncates the bytes back or removes a created file. Rows are ASCII-escaped without NaN, the digest's inputs. `ao prune` never rewrites evidence and replaces logs whole; plan baselines use the locked durable append and fail closed; the helper registry changes under a lock and reading it writes nothing. `tests/test_ledger_atomic.py` injects each failure and asserts the file is unchanged. |
| 75 | When no reviewer or architect can be reached, a second session takes the role | STANDIN-COLLECT (review deferred by the owner, waiver W-1789568159) | On an unreachable reviewer, `ao review` writes `.ao/review-requests/<nonce>.md` (exact prompt, nonce first) and metadata binding it to the candidate. A person runs `ao collect-review <nonce> --response F --model M --by NAME`; ao requires the nonce and unchanged staged bytes, refuses agents, repeats and capability-matrix projects, and records an ordinary review by `human-assisted:<model>` with transport, person and the two limits in its evidence; it counts as a round and can authorise. Ruling against #86's refusal and the architect stand-in are in `docs/standin.md`. `tests/test_standin_collect.py` covers a mismatched nonce, a stale candidate and a slow-but-answering reviewer. |
| 69 | What the watchdog sees reaches someone | ESCALATE-DURABLE (review deferred by the owner, waiver W-1789568161) | Pending mail is matched by role and includes the watchdog's own anomaly reports; a report is owed a wake until handed to a wake whose log shows no transport failure, so a failed wake is retried and a worked one is not repeated. An anomaly with any row in its window is not re-recorded; a failed wake rings a person with masked text; a dead session is resumed only if a different one is found; flags ao adds to the implementer are recorded in `.ao/ledger/actor-flags.jsonl` with the reason. `tests/test_escalate_durable.py` runs each in the scenario world. |
| 18 | Watchdog never reports the architect's own notes as implementer reports | ESCALATE-DURABLE (review deferred by the owner, waiver W-1789568161) | Mail whose sender field is the architect raises no anomaly, is not a standing request, and wakes nobody; a test writes a self-addressed note and proves both. |
| 23 | Pending mail reaches an architect who is present | ESCALATE-DURABLE (review deferred by the owner, waiver W-1789568161) | When a wake is suppressed because the architect session is interactive, the waiting reports reach a person once per newest report; a test with a present architect and a blocked report proves the notice. |
| 20 | A decision request always wakes the architect | DECISION-LADDER (review deferred by the owner, waiver W-1789568162) | Each open decision is its own decision-requested anomaly and is owed a wake through #69's handed set; after 15 minutes it rings a person hourly on the orange ladder (red and mail after its hour) until answered; the implementer is not nudged while a decision is open. `tests/test_decision_ladder.py` proves the ring, the wake past an already-handed report, and the nudge guard. |
| 87 | A wake that cannot happen is collapsed, not queued | DECISION-LADDER (review deferred by the owner, waiver W-1789568162) | An open deferral of a kind is the one that stands; the architect-quota alarm stays quiet until its known reset after one red mail; one wake carries everything when the window reopens, and a handed report never re-raises per tick (#69). A test drives an outage and asserts one deferral, the quiet alarm and exactly one wake after it. |
| 110 | A watchdog cycle runs alone and acts only on what is still true when it acts | CYCLE-SAFETY (review deferred by the owner, waiver W-1789568164) | Audit findings of 2026-09-08 not closed elsewhere. A live cycle takes a per-project lock and stands down when it is held; the heartbeat is written before any early return, a heartbeat silent over a week is a retired project and uninstall removes its own; the hold is re-read before each wake, refill and nudge; after reaping, any agent process the reaper could not have started stops the nudge; the nudged child is identified by its recorded process start; only the architect's mail lifts the round budget; a parked review is not open work and a person is told daily; a refill needs quota and reads its own last failure; PATH uses `os.pathsep` and desktop notifications never raise (#71's PATH and osascript findings). `tests/test_cycle_safety.py` proves each. |
| 66 | Nothing is keyed by a directory's basename | PROJECT-KEY (review deferred by the owner, waiver W-1789571566) | `lib.project_key` names every per-project artefact outside the tree - push window, watchdog state, logs, cycle lock and scheduler jobs, heartbeat, architect lock, helper and reviewer records, ping target, telegram job, alarms. The name belongs to the resolved path in `~/.ao/projects.json` and is recorded with that path in `.ao/project-key`; it is the basename unless a live project holds it (case-insensitively), then basename plus eight hex digits of the path. `ao projects` lists projects sharing a directory name. `tests/test_project_key.py` registers two projects named `api` and proves push windows, watchdog state, heartbeats, locks and records stay apart, plus case, lost registry, copied `.ao/` and retired names. |
| 65 | Round accounting and reviewer selection are honest | ROUND-LEDGER and REVIEWER-HONESTY (review deferred by the owner, waiver W-1789571568) | Rounds are verdict rows of the chained review ledger for the running slice, back to the newest approval that could authorise or the architect's recorded re-specification; `since:` and review files decide nothing. Without a matrix a reviewer is refused for the implementer's session, a shared declared family, or the implementer's engine, in `ao review`, its fallbacks, the probe and `ao commit-ok`. A reviewer's process group dies on timeout; an over-long prompt is refused naming the limit; a review row precedes its file; a carried answer is a fallback. `docs/audit/2026-09-08-closure.md` maps every finding of the cluster to its fix and test or its reason for being declined. |
| 74 | A person can set what the code currently decides for them | SETTINGS and NOTIFY-PROVIDERS (review deferred by the owner, waiver W-1789573946) | `src/ao/settings.py` holds every threshold that differs between people, projects or machines, with its default and scope; the project's config, then `~/.ao/settings.json`, then the default decide, and nothing else does. `ao config list/get/set/unset` reads and writes them whole; `ao doctor` names a value that is set but unusable, a machine setting written into a project and a name ao does not read. `docs/configuration.md` lists them, and tests keep the registry, the documentation and the code in agreement, including an AST check that no module reads a threshold with a default of its own. Mail goes through a provider - formsubmit or any SMTP server - with credentials in `~/.ao/email.json` (0600). |
| 11 | Scenario fuzzing of the guard chain | SCENARIO-FUZZ (review deferred by the owner, waiver W-1789573947) | `tests/test_scenario_fuzz.py` builds forty worlds by seed (more with `AO_FUZZ_SEEDS`) across processes, transcript age, board, mail, decisions, hold, quota, switches, round budget and a present architect, runs a dry cycle in each and fails, printing the seed and the world, on any decision that contradicts the invariants of F1-F19 as documented in `docs/watchdog.md`. |
| 36 | A credit sample knows which account it came from | GOLDEN-VALUES (review deferred by the owner, waiver W-1789573949) | The usage reader returns the account as a short hash of the profile ARN and every credit sample records it; `burn_rate` projects only over the newest reading's account, so a switch starts a new series, and a reading without an account projects nothing; the doctor's credit finding names the account. `tests/test_golden_values.py` has two accounts in one ledger and proves the projection uses only the live one. |
| 41 | Golden values for every clock, quota and counter | GOLDEN-VALUES (review deferred by the owner, waiver W-1789573949) | `tests/test_golden_values.py` tables `parse_reset` (now reading dated weekly resets), `quota_block_until`, `burn_rate`, the doctor's credit finding and `rounds`, with the three 2026-09-07 defects as rows and the awkward cases; each table records its function's branch count, and a function that gains a branch fails until rows are added and the count updated. |
| 19 | Mail file names are filesystem-safe | SAFE-NAMES (review deferred by the owner, waiver W-1789575749) | `lib.safe_slug` keeps only `[A-Za-z0-9._-]` and never returns an empty part; `ao mail send` (topic and type), `ao note`/`ao decide` (title and recipient), the MCP report and the Telegram inbox name files through it. `tests/test_safe_names.py` sends a topic holding `/ \ : * ? " < > |` and whitespace and finds the file in the mailbox. |
| 24 | A tools-disabled reviewer really has no tools | REVIEWER-TOOLS (review deferred by the owner, waiver W-1789575750) | The reviewer argv `ao init` writes (default and `--reviewer-model`) and the one `ao review` suggests carry `--strict-mcp-config` and no `--mcp-config`; `allowlist.reviewer_problems` names a reviewer granted every tool, allowed tools beyond Read/Grep/Glob, or a Claude Code argv that starts MCP servers, and `ao doctor` reports it for the reviewer and each fallback. `tests/test_reviewer_tools.py` proves both generated paths and the doctor finding. |
| 4 | Reviewer verdict and health literals are closed enums | CLOSED-ENUMS (review deferred by the owner, waiver W-1789575751) | `src/ao/verdicts.py` owns `Verdict` (APPROVED, NEEDS_CHANGES, UNAVAILABLE, INVALID) and `ReviewStatus` (pending, complete, invalid, unavailable); verdict parsing uses the enum, so anything else is INVALID, and strict evidence refuses an unknown status when written. The playbook and `docs/protocol.md` list the same values; `tests/test_verdicts.py` holds all three equal and scans the source for literals the enum does not hold. |
| 6 | Reports carry exit codes, not prose counts | REPORT-EVIDENCE (review deferred by the owner, waiver W-1789575753) | Each gate in a verification keeps its exit code and the runner's closing line; `ao_report` appends the newest verification's id, result, exits and lines, and the RAPOR template names it. A report claiming green while the newest verification failed is marked `## INCONSISTENT` by `ao_report` and raised by the watchdog as an `inconsistent-report` anomaly. `tests/test_report_evidence.py` proves the line, the flag in both places and the consistent cases. |
| 16 | Isolation refusal is scoped to the candidate | ISOLATION-SCOPE (review deferred by the owner, waiver W-1789575754) | A gate may declare `inputs` globs; when every gate does, `candidate_worktree_issues` refuses only dirty paths in the candidate or matching a gate's inputs and returns the rest as `worktree_noise`, which commit-ok lists and never refuses on; any gate without inputs keeps every dirty path fatal. `tests/test_isolation_scope.py` grants authority with an unrelated untracked file, refuses one a gate reads, and keeps the old behaviour when a gate declares nothing. |
| 5 | Gate coverage check in `doctor` and `init` | GATE-COVERAGE (review deferred by the owner, waiver W-1789575756) | `ao doctor` reports `gate-uncovered:<tree>` for every top-level tree holding at least `gates.coverage_min_files` files of a toolchain that no gate exercises (runs its runner and, when inputs are declared, reaches the tree); gate detection gives every detected toolchain gates, and `ao init` refuses, before any write, a quick profile exercising none of them unless `--allow-uncovered-gates`. `tests/test_gate_coverage.py` proves the doctor finding, input scoping, multi-toolchain detection and the refusal with its override. |
| 21 | A long review shows it is alive | REVIEW-HEARTBEAT (review deferred by the owner, waiver W-1789575757) | `_run_reviewer` waits in heartbeat-sized `communicate()` calls and prints, every `REVIEW_HEARTBEAT_SECONDS`, the reviewer's id, elapsed time and child pid, then on exit its exit code and wall time; the waits sum to the route timeout, and a reviewer that produces no artefact stays UNAVAILABLE with its exit code. `tests/test_review_heartbeat.py` runs a reviewer that sleeps then exits 3 and asserts both lines, and one that times out after exactly three beats. |
| 37 | A notice carries the evidence that produced it | NOTICE-EVIDENCE (review deferred by the owner, waiver W-1789577886) | Notices have ids; a notice raised on measurements records `{check, samples: [{value, source, at}]}`, built by `lib.notice_evidence`, which refuses a projection without its samples; `notify` carries it to the notice, the alarm episode (`ao alarms`) and the red e-mail; the credit projection and exhaustion alarms raise with it; `ao notices <id>` prints the check and every sample with its age. `tests/test_notice_evidence.py` proves each. |
| 29 | The urgent channel runs both ways, on the commands actually used | URGENT-BOTH-WAYS (review deferred by the owner, waiver W-1789577888) | `urgent_messages` resolves urgency for a role (architect-bound mail is the architect's; `None` takes both); the watchdog sets `AO_ROLE` on the turns it starts; `ao status`, `ao board`, `ao tail` and `ao mail list/read` print marked messages for the invoking role, naming whom each is for, plus a count of the rest; authority checks keep reading the implementer's. `tests/test_urgent_both_ways.py` surfaces a marked architect-bound message in `ao status`. |
| 27 | Reviews are submitted and collected, never waited on | PIPELINE-SUBMIT (review deferred by the owner, waiver W-1789577889) | `ao review submit` pins the staged tree as a private index under `.ao/reviews/`, records `R-<id>.json` and starts the run detached, returning the id at once; the run reviews the pinned tree whatever the live index holds, and a moved HEAD makes it stale; `ao reviews` lists state, slice, elapsed and verdict (a dead runner is `lost`); `ao review collect <id>|--any` never waits; `ao commit-ok --review <id>` grants only on the pinned tree, printing both digests otherwise; `review.max_inflight` (2) and one slice per worktree bound submits. `tests/test_review_submit.py` covers each. |
| 28 | A returned review is handled before new work starts | PIPELINE-HANDLE (review deferred by the owner, waiver W-1789577892) | `lib.returned_reviews` lists submitted reviews that ended and were not collected; `ao status` names each with its slice, verdict and the collect command; the watchdog raises `review-returned` for one uncollected past `review.unhandled_minutes` (30); the playbook states the handling order (returned review, then submit and take READY, then develop; never wait; a parked slice holds no slot, the in-flight limit counting only running reviews). `tests/test_review_returned.py` proves the status line and the anomaly. |
| 82 | The dependency rule is written down as three tiers, and every refusal is checked against it | DEPENDENCY-TIERS (review deferred by the owner, waiver W-1789577894) | `docs/upstream.md` states the tiers as policy and `pyproject.toml`'s comment points at it; `ao doctor` lists keyflip, telegram, e-mail and the ping as installed, configured or absent with what would enable each; `tests/test_dependency_tiers.py` runs `ao status`, `board` and `doctor` in an isolated interpreter and fails if a module outside the standard library and ao is loaded. |
| 95 | A budget that is spent is not a budget that will be spent | 95a (earlier) and SPEND-LABELS (review deferred by the owner, waiver W-1789577896) | The sampler, the first-reading exhaustion alarm, the blind-check doctor line and the launchd PATH closed as #95a. `ao cost` and `ao credits` print the account's figure beside ao's own transcript share, as a percentage of the account's used credits, and say that the account counts every session on it while ao's transcript holds only ao's turns. `tests/test_spend_labels.py` proves both. |
| 92a | A notice is held for the architect only when a wake is coming | HELD-NOTICES (review deferred by the owner, waiver W-1789488409) | escalate() held every anomaly notice for the architect whenever an architect argv was configured; on 2026-09-14 seven notices sat held behind "architect will be woken" while the only architect was an interactive session nobody was prompting, and the owner learned of the blocked implementer by asking. `architect_hold_reason()` now answers the hold question from the wake's own predicates and holds only for a wake in flight or one this cycle will start; otherwise the notice goes to a person as "needs you" with the reason no architect will act. Watchdog titles name the project instead of a hard-coded "Voltrai", and title-based audience inference reads the subject after the project prefix, so a project's name cannot change where its alerts go. Four tests in `tests/test_watchdog_cycle.py`. |
| 95a | The credit check can see usage, and says so when it cannot | CREDITS-SAMPLER (review deferred by the owner, waiver W-1789487998) | `kiro_account_usage` resolves `kiro-cli` through the harness binary search instead of the ambient PATH and returns an error naming the failed step instead of None. The watchdog sampler moves into `_sample_credits`: it tries at most once per half hour whether or not the reading succeeds, records a failed reading as a broken check, and raises exhaustion from the reading itself on the first observation. Doctor always prints a blind check and pages it only while the implementer is being driven. The watchdog and doctor launchd jobs and the telegram poller are installed with a PATH that keeps existing directories and drops shell-bound version-manager ones. Six tests in `tests/test_watchdog_cycle.py`. |
